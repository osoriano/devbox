---
- name: Update git user name
  git_config:
    scope: global
    name: user.name
    value: "{{ git_user_name }}"

- name: Update git user email
  git_config:
    scope: global
    name: user.email
    value: "{{ git_user_email }}"
